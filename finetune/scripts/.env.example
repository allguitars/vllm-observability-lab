# 個別訓練批次的持久化輸出目錄。
# 將此檔複製為 .env 後，為每次訓練設定新的目錄名稱，例如：
# RUN_DIR=/workspace/finetune/runs/gemma3-12b-smoke-002
RUN_DIR=/workspace/finetune/runs/gemma3-12b-smoke-001

# RUN_DIR 僅控制 run_finetune.sh 寫入的開始／結束時間、總耗時、退出碼與 wrapper log。
# 同時修改 configs/env/env_config.yaml，讓下列欄位使用相同的 run 目錄：
# output_dir: '/workspace/finetune/runs/<本次目錄>/trained-model'
# log_name: '/workspace/finetune/runs/<本次目錄>/train.log'
# 否則模型與群聯訓練 log 會被寫到不同的目錄。

# 限制本腳本及其啟動的 phisonai2 僅使用 container 的 cuda:0。
# 目前 cuda:0 已確認為 GPU 0 的第一個 3g.71gb MIG。
# 這是訓練程序層的限制；不代表 container 的 MIG 隔離已驗證成功。
export CUDA_VISIBLE_DEVICES=0
